<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 메인 테마: 반드시 AppCompat 또는 MaterialComponents 기반 -->
    <style name="Theme.HealthSpoon" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    </style>

    <!-- 스플래시 테마 -->
    <style name="Theme.HealthSpoon.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_spoon</item> <!-- 필요 시 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="postSplashScreenTheme">@style/Theme.HealthSpoon</item> <!-- Android 12+ 전환 테마 -->
    </style>

</resources>